# Update {{name}}

Updates `{{name}}` formula to version {{version}}

- **Version**: {{version}}
- **SHA256**: {{sha256}}

This is an automated update triggered by a new release in the upstream repository.

Fixes #{{issue_number}}
